# qa-specific values.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-management-app-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: APP_ENV
              value: qa
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-management-app-ui
spec:
  template:
    spec:
      containers:
        - name: ui
          env:
            - name: APP_ENV
              value: qa
---
apiVersion: batch/v1
kind: Job
metadata:
  name: user-management-app-db-migrate
spec:
  template:
    spec:
      containers:
        - name: migrate
          env:
            - name: APP_ENV
              value: qa
---
apiVersion: v1
kind: Service
metadata:
  name: user-management-app-ui
  annotations:
    # → user-management-qa.eastus2.cloudapp.azure.com
    service.beta.kubernetes.io/azure-dns-label-name: user-management-qa
---
# own JWT signing secret — see base/externalsecret-jwt.yaml's header comment for why
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: user-management-app-jwt
spec:
  data:
    - secretKey: secret
      remoteRef:
        key: user-management-app/jwt-qa
        property: secret
---
# qa's own backup container — see base/objectstore.yaml's header comment for the
# azure-storage-creds Secret this depends on (created once, manually, not in git)
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: user-management-app-db-store
spec:
  configuration:
    destinationPath: "https://CHANGEME.blob.core.windows.net/postgres-backups-qa"
