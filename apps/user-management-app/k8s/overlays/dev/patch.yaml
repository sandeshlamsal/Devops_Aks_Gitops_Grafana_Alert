# dev-specific values.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-management-app-api
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: APP_ENV
              value: dev
---
apiVersion: v1
kind: Service
metadata:
  name: user-management-app-ui
  annotations:
    # → user-management-dev.eastus2.cloudapp.azure.com  (stable public URL)
    service.beta.kubernetes.io/azure-dns-label-name: user-management-dev
