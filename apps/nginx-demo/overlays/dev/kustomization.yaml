# Dev overlay: the base plus a replica-count patch. This is the path Flux's "apps"
# Kustomization points at.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ../../base
patches:
  - path: replica-patch.yaml
