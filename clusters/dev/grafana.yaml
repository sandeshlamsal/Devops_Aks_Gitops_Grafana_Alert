# WHY: tells Flux to reconcile infrastructure/grafana/ — the Grafana instance +
# Prometheus datasource + dashboards. dependsOn: grafana-operator (its CRDs must be
# registered first) and secrets (the grafana-admin Secret must exist so the pod starts).
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grafana
  namespace: flux-system
spec:
  interval: 10m
  path: ./infrastructure/grafana
  prune: true
  dependsOn:
    - name: grafana-operator
    - name: secrets
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: monitoring
