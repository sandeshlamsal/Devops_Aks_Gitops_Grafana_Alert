# Applied by the Flux Kustomization "external-secrets-operator".
# Kept separate from ../stores + ../externalsecrets so its CRDs register first.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - external-secrets-operator.yaml
