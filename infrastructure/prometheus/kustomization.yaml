# Applied by the Flux Kustomization "prometheus" (clusters/dev/prometheus.yaml).
# The "grafana" and "alert" Kustomizations both dependsOn this one.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - kube-prometheus-stack.yaml
